{
  "scripts": {
    "dev": "concurrently \"cd test-backend && npm run start\" \"cd frontend && npm run dev\"",
    "backend": "cd test-backend && npm run start",
    "setup": "yarn install && cd test-backend && yarn install && cd ../frontend && yarn install"
  },
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
